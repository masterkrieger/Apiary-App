name: Deploy to Server
run-name: ${{ github.actor }} is deploying app with GitHub Actions 🚀
on:
  push:
    branches:
      - main    # Adjust the branch name as per your project's main branch

jobs:
  deploy:
    runs-on: ubuntu-latest    # Choose the appropriate runner environment

    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'    # Specify the desired Node.js version

      - name: Install dependencies
        run: npm ci            # Use `npm install` if you're not committing `package-lock.json`

      - name: Build the app
        run: npm run build     # Adjust the build script as per your project's configuration

      - name: Deploy to server
        run: |
          ssh grid@your-server 'cd /var/www/html/ && git pull'
          # Additional commands to start/restart your app on the server if needed

      # Add more steps if required, such as running tests or notifications
